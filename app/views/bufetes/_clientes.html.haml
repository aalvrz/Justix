%h3 Clientes

/ Show all Clients from this Bufete
.row
    - @bufete.clientes.each do |cliente|
        .col-sm-12.col-md-3.text-center    
            = link_to bufete_persona_path(@bufete, cliente) do
                = image_tag cliente.photo.url(:thumb), class: 'img-circle'
                
                %h5
                    = cliente.nombre_completo

.row.text-center
    = link_to new_bufete_persona_path(@bufete) do
        %span.glyphicon.glyphicon-plus
        Crear Nuevo Cliente