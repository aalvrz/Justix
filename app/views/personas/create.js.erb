$(function () {
    $('#new_persona_modal').modal('hide');
    
    $("#caso_cliente_tokens").tokenInput("add", {
        id: '<%= @persona.id %>',
        nombre_completo: '<%= @persona.nombre_completo %>'
    });
});